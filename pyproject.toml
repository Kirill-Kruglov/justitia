[project]
name = "justitia"
version = "0.1.0"
description = "A simulation of blind governance: consequence-gated anti-concentration"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
