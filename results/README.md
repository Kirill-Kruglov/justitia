# Published evidence

Curated outputs from the full study (the multi-gigabyte raw per-run table is not
checked in; everything here is regenerable via `python run.py`).

| File | What it shows |
|---|---|
| `boundary_atlas.md` | Where the robust kernel breaks along each pressure axis |
| `sensitivity_report.md` | Threshold-stability + the **Lever Coupling** result (verdict BE) |
| `run_manifest.json` | Git head, spec hash, validation gates, final verdict |
| `ac_vs_cg_study.md` | The premise: static caps alone and consequence response alone both fail |
| `*.svg` | Permanence-vs-axis curves and marginal-gap charts per world |
| `raw/*.csv` | Summarized cells, boundary brackets, decoupling, coupling ablation |
